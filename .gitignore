# Craft Choreographer workflow state (user-local; do not commit)
.craft/state.json

# Local feedback log (append-only)
.craft/feedback.jsonl

# Local archive (design references; not published)
docs/archive/
